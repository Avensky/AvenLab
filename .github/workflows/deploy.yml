name: Deploy AvenLab

on:
  push:
    branches:
      - main

concurrency:
  group: avenlab-deploy
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up NodeJS
        uses: actions/setup-node@v4
        with:
          node-version-file: ".nvmrc"
          cache: "npm"

      - name: Install root dependencies
        run: npm ci

      - name: Install frontend dependencies
        run: npm ci --prefix frontend

      - name: Pull Git LFS files
        run: |
          git lfs install
          git lfs pull

      - name: Verify GLB files are real
        run: |
          file frontend/public/models/vehicles/ae86.glb
          head -c 20 frontend/public/models/vehicles/ae86.glb

      - name: Install Rust toolchain
        shell: bash
        run: |
          set -eux
          if [ ! -x "$HOME/.cargo/bin/rustc" ]; then
            curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y
          fi
          echo "$HOME/.cargo/bin" >> "$GITHUB_PATH"
          export PATH="$HOME/.cargo/bin:$PATH"
          rustc --version
          cargo --version

      - name: Cache Rust build
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: avenlab-server

      - name: Run project checks
        shell: bash
        run: |
          export PATH="$HOME/.cargo/bin:$PATH"
          npm run check

      - name: Build frontend
        run: npm --prefix frontend run build

      - name: Build Rust physics server outside workspace
        shell: bash
        run: |
          export PATH="$HOME/.cargo/bin:$PATH"
          cd avenlab-server
          cargo build --release --target-dir /tmp/avenlab-rust-build

      - name: Install runtime files
        run: |
          sudo mkdir -p /var/www/avenlab/bin
          sudo mkdir -p /var/www/avenlab/current/frontend
          sudo mkdir -p /var/www/avenlab/current/data-server

          sudo rsync -a --delete frontend/dist/ /var/www/avenlab/current/frontend/dist/
          sudo rsync -a --delete --exclude '.venv' data-server/ /var/www/avenlab/current/data-server/

          sudo install -m 755 /tmp/avenlab-rust-build/release/avenlab-server /var/www/avenlab/bin/avenlab-server

      - name: Install production data-server venv
        run: |
          cd /var/www/avenlab/current/data-server
          rm -rf .venv
          python3 -m venv .venv
          .venv/bin/python -m pip install --upgrade pip
          .venv/bin/python -m pip install -r requirements.txt

      - name: Restart physics service
        run: |
          sudo systemctl restart avenlab-physics
          sleep 2
          sudo systemctl is-active --quiet avenlab-physics

      - name: Restart data server
        run: |
          sudo systemctl restart avenlab-data
          sleep 2
          sudo systemctl is-active --quiet avenlab-data

      - name: Test and reload NGINX
        run: |
          sudo nginx -t
          sudo systemctl reload nginx

      - name: Health check frontend
        run: |
          curl -f http://127.0.0.1/ > /dev/null

      - name: Health check physics port
        run: |
          ss -ltn | grep ':9001'

      - name: Optional data-server health check
        continue-on-error: true
        run: |
          systemctl is-active --quiet avenlab-data-server
          curl -f http://127.0.0.1:8001/health

      - name: Optional Ollama health check
        continue-on-error: true
        run: |
          curl -f http://127.0.0.1:11434/api/tags

      - name: Health check virtual CAN
        continue-on-error: true
        run: |
          ip link show vcan0

      - name: Health check physical CAN
        continue-on-error: true
        run: |
          ip link show can0
          ip link show can1
